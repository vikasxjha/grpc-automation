# Stage Environment Configuration
grpc.host=stage.example.com
grpc.port=443
grpc.tls.enabled=true
grpc.tls.cert.path=/path/to/cert.pem

auth.enabled=true
auth.token=${AUTH_TOKEN}

logging.request.enabled=true
logging.response.enabled=false

